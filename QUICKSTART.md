# Быстрый старт Grid Guardian

## 🚀 Для нетерпеливых

### Windows (3 простых шага):

```bash
1. install.bat       # Установка (5-10 минут)
2. test_generator.bat  # Тест с маленьким датасетом (1 минута)
3. dashboard.bat     # Запуск дашборда
```

## 📋 Пошаговая инструкция

### Шаг 1: Установка зависимостей

**Запустите:**
```bash
install.bat
```

Этот скрипт:
- ✅ Создаст виртуальное окружение Python
- ✅ Установит все библиотеки (pandas, tensorflow, streamlit и т.д.)
- ⏱️ Займет 5-10 минут

**Что делать если ошибка:**
- Убедитесь, что Python 3.9+ установлен: `python --version`
- Скачайте Python: https://www.python.org/downloads/
- При установке отметьте "Add Python to PATH"

---

### Шаг 2: Тест генератора (опционально)

**Запустите:**
```bash
test_generator.bat
```

Это быстро проверит работу генератора:
- Создаст **8,400 строк** тестовых данных (50 устройств × 168 часов)
- Займет **~30 секунд**
- Файл: `data/test/grid_telemetry_data.parquet` (~1 MB)

---

### Шаг 3: Генерация полного датасета

**Вариант A: Полный датасет (87.6M строк)**
```bash
# Активировать окружение
venv\Scripts\activate

# Запустить генератор
python data/generate_data.py
```

**Параметры:**
- 500 подстанций × 10 оборудования = 5,000 объектов
- 2 года почасовых данных = 17,520 часов
- **Итого: 87,600,000 строк**
- **Размер: ~1.5-2 GB** (Parquet со сжатием)
- **Время: 20-40 минут**

**Вариант B: Уменьшенный датасет (для теста)**

Отредактируйте `data/generate_data.py` внизу файла:
```python
# Строки 396-398
N_SUBSTATIONS = 50              # Было: 500
EQUIPMENT_PER_SUBSTATION = 10   # Было: 10
HOURS = 8760                    # Было: 17520 (1 год вместо 2)
```

Это создаст **4,380,000 строк** (~200 MB, 5-10 минут)

---

### Шаг 4: Запуск полного пайплайна

**Запустите:**
```bash
start.bat
```

Это выполнит:
1. ✅ Генерацию данных
2. ✅ Предобработку
3. ✅ Feature engineering
4. ✅ Обучение XGBoost модели
5. ✅ Обучение LSTM модели

⏱️ **Время:** 30-60 минут для полного датасета

**Или ручной запуск пайплайна:**
```bash
venv\Scripts\activate
python run_pipeline.py
```

---

### Шаг 5: Запуск дашборда

**Запустите:**
```bash
dashboard.bat
```

Или вручную:
```bash
venv\Scripts\activate
streamlit run app/Home.py
```

**Откроется браузер:** http://localhost:8501

**Страницы дашборда:**
- 🏠 **Home** - Обзор и ключевые метрики
- 📊 **Monitoring** - Мониторинг в реальном времени
- 🔮 **Predictions** - AI-прогнозы отказов
- 💰 **Financial** - Финансовый анализ
- 🗺️ **Maps** - Карта оборудования

---

## 🛠️ Доступные скрипты

| Скрипт | Описание | Время |
|--------|----------|-------|
| `install.bat` | Установка зависимостей | 5-10 мин |
| `test_generator.bat` | Тест генератора (8K строк) | 30 сек |
| `start.bat` | Полный пайплайн | 30-60 мин |
| `dashboard.bat` | Запуск дашборда | мгновенно |
| `quickstart.bat` | Всё сразу (устарел, используйте install+start) | 40-70 мин |

---

## 📂 Структура данных

### Сгенерированные файлы:

```
data/
├── raw/
│   ├── grid_telemetry_data.parquet   # 87.6M строк, ~1.5-2 GB
│   └── equipment_locations.parquet   # 5K строк, ~100 KB
├── processed/
│   ├── cleaned_data.csv              # Очищенные данные
│   └── features.csv                  # Признаки для ML
└── test/
    └── grid_telemetry_data.parquet   # Тестовые данные (если запущен test_generator.bat)
```

### Колонки телеметрии (18 штук):

| Колонка | Тип | Описание |
|---------|-----|----------|
| timestamp | datetime | Время измерения |
| equipment_id | string | ID оборудования (SUB001_EQ01) |
| temperature_top | float | Температура верха (°C) |
| temperature_oil | float | Температура масла (°C) |
| voltage_phase_a/b/c | float | Напряжение фаз A/B/C (kV) |
| current_phase_a/b/c | float | Ток фаз A/B/C (A) |
| gas_h2 | float | Растворенный водород (ppm) |
| gas_ch4 | float | Растворенный метан (ppm) |
| gas_c2h2 | float | Растворенный ацетилен (ppm) |
| vibration_x/y/z | float | Вибрация по осям (mm/s) |
| humidity | float | Влажность (%) |
| load_percentage | float | Загрузка (%) |

---

## 🐛 Решение проблем

### Python не найден
```
ERROR: Python is not installed or not in PATH
```

**Решение:**
1. Установите Python 3.9+: https://www.python.org/downloads/
2. При установке отметьте "Add Python to PATH"
3. Перезапустите командную строку

---

### Ошибка при установке TensorFlow
```
ERROR: Could not find a version that satisfies the requirement tensorflow
```

**Решение (Windows):**
```bash
# Активируйте окружение
venv\Scripts\activate

# Установите TensorFlow отдельно
pip install tensorflow-cpu
```

Или используйте `tensorflow-cpu` вместо `tensorflow` в `requirements.txt`

---

### Не хватает памяти при генерации
```
MemoryError
```

**Решение:**
Уменьшите размер датасета в `data/generate_data.py`:
```python
N_SUBSTATIONS = 50     # Вместо 500
HOURS = 4380           # 6 месяцев вместо 2 лет
```

---

### Ошибка "No module named 'pyarrow'"
```bash
venv\Scripts\activate
pip install pyarrow fastparquet
```

---

### Dashboard не открывается
```bash
# Проверьте, не занят ли порт 8501
netstat -ano | findstr :8501

# Используйте другой порт
streamlit run app/Home.py --server.port 8502
```

---

## 📊 Ожидаемая производительность

### Размеры файлов:

| Датасет | Строк | CSV | Parquet |
|---------|-------|-----|---------|
| Тест | 8,400 | ~2 MB | ~1 MB |
| Маленький (50 суб, 1 год) | 4.4M | ~900 MB | ~150 MB |
| Полный (500 суб, 2 года) | 87.6M | ~18 GB | ~1.5-2 GB |

### Время генерации (типичный ноутбук):

| Датасет | Время | RAM |
|---------|-------|-----|
| Тест (8K) | 30 сек | 500 MB |
| Маленький (4.4M) | 5-10 мин | 2 GB |
| Полный (87.6M) | 20-40 мин | 2-4 GB |

---

## 🎯 Что дальше?

После генерации данных:

1. **Изучите данные:**
   ```python
   import pandas as pd
   df = pd.read_parquet('data/raw/grid_telemetry_data.parquet')
   print(df.head())
   print(df.describe())
   ```

2. **Запустите препроцессинг:**
   ```bash
   python src/preprocessing.py
   ```

3. **Создайте признаки:**
   ```bash
   python src/feature_engineering.py
   ```

4. **Обучите модели:**
   ```bash
   python models/train_xgboost.py
   python models/train_lstm.py
   ```

5. **Или запустите всё сразу:**
   ```bash
   start.bat  # или python run_pipeline.py
   ```

---

## 📚 Документация

- **[README.md](README.md)** - Полная документация проекта
- **[DATA_GENERATION_GUIDE.md](DATA_GENERATION_GUIDE.md)** - Детальное описание генератора
- **[PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - Структура проекта

---

## ✅ Чеклист первого запуска

- [ ] Python 3.9+ установлен и в PATH
- [ ] Запущен `install.bat` (зависимости установлены)
- [ ] Запущен `test_generator.bat` (тест прошел успешно)
- [ ] Запущен `python data/generate_data.py` (данные сгенерированы)
- [ ] Запущен `start.bat` или `python run_pipeline.py` (модели обучены)
- [ ] Запущен `dashboard.bat` (дашборд открылся в браузере)
- [ ] Изучены страницы: Home, Monitoring, Predictions, Financial, Maps

**Поздравляем! Система работает! 🎉**

---

## 💡 Советы

- **SSD диск** ускорит генерацию в 3-5 раз
- **Больше RAM** позволит увеличить batch size
- **Python 3.11+** быстрее Python 3.9
- Закройте другие приложения при генерации больших датасетов
- Используйте **test_generator.bat** перед полной генерацией

---

**Вопросы?** Читайте [README.md](README.md) или создайте issue на GitHub.
