# Руководство по установке Grid Guardian

## 🎯 Три варианта запуска

### Вариант 1: Минимальный (быстрый тест)
**Время:** 15-20 минут
**Датасет:** ~8,400 строк

```bash
install.bat              # 10 мин - установка
test_generator.bat       # 30 сек - генерация тестовых данных
dashboard.bat            # запуск дашборда
```

---

### Вариант 2: Средний (пайплайн)
**Время:** 30-40 минут
**Датасет:** ~1M строк (50 подстанций, 3 месяца)

```bash
install.bat              # 10 мин - установка
start.bat                # 20-30 мин - генерация + обучение моделей
dashboard.bat            # запуск дашборда
```

---

### Вариант 3: Полный (production)
**Время:** 50-70 минут
**Датасет:** 87.6M строк (500 подстанций, 2 года)

```bash
install.bat              # 10 мин - установка

# Активировать окружение
venv\Scripts\activate

# Полная генерация
python data/generate_data.py  # 30-40 мин

# Обучение моделей (опционально)
python models/train_xgboost.py
python models/train_lstm.py

# Дашборд
dashboard.bat
```

---

## 📊 Сравнение вариантов

| Параметр | Минимальный | Средний | Полный |
|----------|-------------|---------|--------|
| **Подстанций** | 10 | 50 | 500 |
| **Оборудования** | 50 | 500 | 5,000 |
| **Период** | 1 неделя | 3 месяца | 2 года |
| **Строк** | 8,400 | ~1,080,000 | 87,600,000 |
| **Размер файла** | ~1 MB | ~40 MB | ~1.5-2 GB |
| **Время генерации** | 30 сек | 3-5 мин | 30-40 мин |
| **Общее время** | 15 мин | 35 мин | 65 мин |
| **Подходит для** | Тест, демо | Разработка | Production |

---

## 🚀 Рекомендуемый путь

### Для первого запуска:

**Шаг 1:** Быстрый тест
```bash
install.bat
test_generator.bat
dashboard.bat
```
✓ Убедитесь, что всё работает (15 минут)

**Шаг 2:** Средний датасет
```bash
start.bat
```
✓ Обучите модели на реалистичном датасете (20 минут)

**Шаг 3:** Полный датасет (если нужен)
```bash
venv\Scripts\activate
python data/generate_data.py
```
✓ Используйте для production (40 минут)

---

## ⚙️ Настройка размера датасета

### В `run_pipeline.py`:

```python
# Строки 25-27
N_SUBSTATIONS = 50           # Измените на 500 для полного
EQUIPMENT_PER_SUBSTATION = 10
HOURS = 2160                 # Измените на 17520 для 2 лет
```

### В `data/generate_data.py`:

```python
# Строки 396-398
N_SUBSTATIONS = 500              # Полный датасет
EQUIPMENT_PER_SUBSTATION = 10
HOURS = 17520  # 2 года
```

---

## 🔧 Конфигурации для разных целей

### Быстрый тест (1 минута генерации):
```python
N_SUBSTATIONS = 10
HOURS = 168  # 1 неделя
# Результат: 8,400 строк, ~1 MB
```

### Разработка (5 минут генерации):
```python
N_SUBSTATIONS = 50
HOURS = 2160  # 3 месяца
# Результат: ~1M строк, ~40 MB
```

### Демо (15 минут генерации):
```python
N_SUBSTATIONS = 100
HOURS = 8760  # 1 год
# Результат: ~9M строк, ~350 MB
```

### Production (40 минут генерации):
```python
N_SUBSTATIONS = 500
HOURS = 17520  # 2 года
# Результат: 87.6M строк, ~1.5-2 GB
```

---

## 📝 Пошаговая инструкция

### 1. Установка

```bash
install.bat
```

**Что происходит:**
- Создаётся виртуальное окружение `venv/`
- Устанавливается pip
- Устанавливаются все пакеты из `requirements.txt`

**Требования:**
- Python 3.9+
- 5 GB свободного места
- Интернет для загрузки пакетов

---

### 2. Тест генератора

```bash
test_generator.bat
```

**Что происходит:**
- Генерируется 8,400 строк тестовых данных
- Сохраняется в `data/test/grid_telemetry_data.parquet`
- Проверяется корректность данных

**Результат:**
```
✓ Successfully loaded 8,400 rows
✓ TEST COMPLETED SUCCESSFULLY!
```

---

### 3. Запуск дашборда

```bash
dashboard.bat
```

**Что происходит:**
- Запускается Streamlit сервер
- Открывается браузер на http://localhost:8501
- Дашборд работает с имеющимися данными

**Страницы:**
- 🏠 Home - Обзор
- 📊 Monitoring - Мониторинг сенсоров
- 🔮 Predictions - AI-прогнозы
- 💰 Financial - Финансовый анализ
- 🗺️ Maps - Карта оборудования

---

### 4. Полный пайплайн

```bash
start.bat
```

**Что происходит:**
1. Генерация данных (50 подстанций, 3 месяца)
2. Предобработка
3. Feature engineering
4. Обучение XGBoost
5. Обучение LSTM

**Время:** 20-30 минут

---

### 5. Полная генерация (опционально)

```bash
venv\Scripts\activate
python data/generate_data.py
```

**Что происходит:**
- Генерируется 87.6M строк
- Показывается прогресс-бар
- Сохраняется в Parquet со сжатием

**Прогресс:**
```
Degradation patterns: 100%|████████| 5000/5000
Generating batches: 100%|████████| 104/104
✓ Data generation complete!
  Total rows written: 87,600,000
  File size: 1.52 GB
```

---

## 🐛 Решение проблем

### Python не найден

**Ошибка:**
```
Python is not installed or not in PATH
```

**Решение:**
1. Скачайте Python 3.9+ с https://www.python.org/downloads/
2. При установке отметьте **"Add Python to PATH"**
3. Перезапустите командную строку

---

### Ошибка при установке TensorFlow

**Ошибка:**
```
Could not find a version that satisfies tensorflow
```

**Решение:**
```bash
venv\Scripts\activate
pip install tensorflow-cpu
```

Или в `requirements.txt` замените:
```
tensorflow>=2.15.0
```
на:
```
tensorflow-cpu>=2.15.0
```

---

### Не хватает памяти

**Ошибка:**
```
MemoryError
```

**Решение:** Уменьшите датасет

В `data/generate_data.py`:
```python
N_SUBSTATIONS = 50    # Вместо 500
HOURS = 4380          # 6 месяцев вместо 2 лет
```

---

### PyArrow не установлен

**Ошибка:**
```
No module named 'pyarrow'
```

**Решение:**
```bash
venv\Scripts\activate
pip install pyarrow fastparquet
```

---

### Порт 8501 занят

**Ошибка:**
```
Port 8501 is in use
```

**Решение:**
```bash
venv\Scripts\activate
streamlit run app/Home.py --server.port 8502
```

---

## 💾 Структура после установки

```
grid-guardian/
├── venv/                           # Виртуальное окружение
├── data/
│   ├── raw/
│   │   ├── grid_telemetry_data.parquet
│   │   └── equipment_locations.parquet
│   ├── processed/
│   │   ├── cleaned_data.csv
│   │   └── features.csv
│   └── test/
│       └── grid_telemetry_data.parquet  # Если запущен test_generator.bat
├── models/
│   └── saved/
│       ├── xgboost_model_latest.pkl
│       ├── lstm_model_latest.h5
│       └── ...
└── logs/
    └── grid_guardian.log
```

---

## 📈 Мониторинг прогресса

### При генерации данных:

```
Configuration:
  Substations: 500
  Total equipment: 5,000
  Hours of data: 17,520 (2.0 years)
  Total rows to generate: 87,600,000

Degradation patterns: 100%|████████████| 5000/5000
Equipment with failures: 251/5000 (5.0%)

Generating batches: 100%|████████████| 104/104
✓ Data generation complete!
  Total rows written: 87,600,000
  File size: 1.52 GB
```

### При обучении моделей:

```
Training XGBoost model...
Training samples: 70,080,000
Testing samples: 17,520,000
ROC-AUC Score: 0.9543
F1 Score: 0.8821
```

---

## 🎓 Следующие шаги

После успешной установки:

1. **Изучите данные:**
   ```python
   import pandas as pd
   df = pd.read_parquet('data/raw/grid_telemetry_data.parquet')
   print(df.head())
   ```

2. **Настройте параметры** в `config/settings.py`

3. **Обучите модели** на полных данных

4. **Интегрируйте** с реальными данными

5. **Разверните** в production

---

## 📚 Дополнительная документация

- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт (RU)
- [README.md](README.md) - Полная документация (EN)
- [DATA_GENERATION_GUIDE.md](DATA_GENERATION_GUIDE.md) - Генератор данных
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Структура проекта

---

**Готовы начать?** Запустите: `install.bat`
